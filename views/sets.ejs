<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Collection</title>
    <link rel="stylesheet" href="/css/main.css" />
  </head>
  <body>
    <%- include('partials/navbar', {page: "/lego/sets"}) %>

    <div class="container mx-auto">
      <div class="hero bg-base-200 rounded-lg p-6">
        <div class="hero-content text-center">
          <div class="max-w-md">
            <h1 class="text-5xl font-bold">Collections</h1>
         
            <p class="py-6">Click the theme to filter by themes such as: <br><a href="/lego/sets?theme=Technic" class="btn btn-primary btn-xs">Technic</a> <a href="/lego/sets?theme=Classic Town" class="btn btn-primary btn-xs">Classic Town</a>  <a href="/lego/sets?theme=lion Knights" class="btn btn-primary btn-xs">Lion Knights</a> </p>

          </div>
        </div>
      </div>
    </div>
<div class="container mx-auto">
    <div class="overflow-x-auto">
      <table class="table table-zebra">
        <tbody>
          <% sets.forEach(set => { %>
            <tr>
              <td>
                <div class="avatar">
                  <div class="w-24 rounded">
                    <img src="<%= set.img_url %>" alt="Image not Available" />
                  </div>
                </div>
              </td>
              <td>
                <%= set.name %><%= ' | ' %><a class="btn  mt-2 btn-xs" href="/lego/sets?theme=<%= set.theme %>"><%= set.theme %></a>
              </td>
              <td>
                <strong><%= 'Year: ' %></strong><%= set.year %><strong><%= ' - Parts: '%></strong><%= set.num_parts %>
              </td>
              <td>
                <button>
                  <a class="btn  mt-2 btn-xs" href="/lego/sets/<%= set.set_num %>">Details</a>
                </button>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
</div>
  </body>
</html>
